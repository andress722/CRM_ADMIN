// Página de etiquetas/packing/picking do admin, integrando PackingPanel
import React from 'react';
import PackingPanel from '../components/PackingPanel';

export default function PackingPage() {
  return (
    <main>
      <PackingPanel />
    </main>
  );
}
